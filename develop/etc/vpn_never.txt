# vpn_never.txt — список направлений, которые ВСЕГДА идут НАПРЯМУЮ
# (никогда через VPN).
#
# Установка/обновление НЕ перезаписывают уже существующий
# /opt/susanin/etc/vpn_never.txt — правьте его свободно.
#
# Формат: по одному домену (или IPv4, или CIDR a.b.c.d/n) на строку.
# '#' — комментарии. Поддерживается wildcard: *.example.com — домен и поддомены.
# Поддомены можно указывать и отдельными строками.
# Приоритет: этот список старше автообучения — адреса из него не попадают
# в ok/test и не маркируются для VPN; для них в цепочке SUSANIN стоит RETURN.
# Если зона есть и в vpn_always.txt, приоритет у этого списка (напрямую).
#
# По умолчанию включён набор для мессенджера MAX (ru.oneme.app): он определяет
# «включён ли VPN» по адресу выхода, поэтому должен ходить напрямую. Набор собран
# по дампу трафика приложения. Также включён набор Битрикс24 / 1С-Битрикс (портал
# и мессенджер) — см. отдельный блок ниже. Файл при обновлении не перезаписывается:
# на уже установленных роутерах строки ниже нужно добавить вручную (или удалить,
# если не нужны).
#
# Важно про wildcard здесь: в vpn_never «*.domain» резолвит только apex и пару
# случайных поддоменов, поэтому для сервисов с обычным DNS надёжнее явные имена
# (api.oneme.ru и т.п.), а не «*.oneme.ru».

# --- Битрикс24 / 1С-Битрикс: портал и мессенджер (im/push) — всегда напрямую ---
# Автообучение ложно заворачивало фронты/CDN Битрикса в VPN: «не открывались
# сообщения», приложение зависало на загрузке, а портал отвечал 502 в момент
# заворота. Пулы CDN меняют адреса, поэтому перечислены имена и сети. Если
# сервис есть и в vpn_always — приоритет у этого списка («напрямую»).
bitrix24.ru
bitrix24.com
1c-bitrix.ru
bitrix.info
cdn.bitrix24.ru
static.bitrix24.ru
cloud.bitrix24.ru
dl.bitrix24.ru
files.bitrix24.ru
im.bitrix24.ru
push.bitrix24.ru
46.235.53.0/24
178.132.201.0/24
178.248.232.0/21

# --- MAX / Oneme: API и внутренние сервисы приложения ---
a.oneme.ru
api.oneme.ru
api2.oneme.ru
au.oneme.ru
fd2.oneme.ru
i.oneme.ru

# --- MAX: CDN и медиа (VK / Mail.ru / OK) ---
*.okcdn.ru
calls.okcdn.ru
iv.okcdn.ru
omu.okcdn.ru
omub.okcdn.ru
ovu.mycdn.me
vu.mycdn.me
vu.okcdn.ru
tracker-api.vk-analytics.ru

# --- проверка сети и адреса выхода ---
ipv4-internet.yandex.net
trace-flow.ru

# --- интеграции ---
gosuslugi.ru
